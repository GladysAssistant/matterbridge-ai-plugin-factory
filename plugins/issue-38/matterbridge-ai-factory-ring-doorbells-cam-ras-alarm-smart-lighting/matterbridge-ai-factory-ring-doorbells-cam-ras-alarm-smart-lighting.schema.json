{
  "title": "Matterbridge Ring plugin",
  "description": "matterbridge-ai-factory-ring-doorbells-cam-ras-alarm-smart-lighting v. 1.0.0",
  "type": "object",
  "properties": {
    "name": {
      "title": "Plugin Name",
      "description": "Plugin name",
      "type": "string",
      "readOnly": true,
      "ui:widget": "hidden"
    },
    "refreshToken": {
      "title": "Ring Refresh Token",
      "description": "Ring refresh token. Generate it once with: npx -p ring-client-api ring-auth-cli (handles email/password + 2FA). Stored persistently to avoid re-login and Ring rate limits.",
      "type": "string"
    },
    "locationModePollingSeconds": {
      "title": "Location Mode Polling (seconds)",
      "description": "How often to poll the Ring location alarm mode.",
      "type": "number",
      "default": 20
    },
    "type": {
      "title": "Plugin Type",
      "description": "Plugin type",
      "type": "string",
      "readOnly": true,
      "ui:widget": "hidden"
    },
    "whiteList": {
      "title": "White List",
      "description": "Only the devices in the list will be exposed. If the list is empty, all the devices will be exposed.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "selectFrom": "name"
    },
    "blackList": {
      "title": "Black List",
      "description": "The devices in the list will not be exposed. If the list is empty, no devices will be excluded.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "selectFrom": "name"
    },
    "debug": {
      "title": "Enable Debug",
      "description": "Enable debug mode for the plugin. Only for debugging purposes, not recommended for production use.",
      "type": "boolean",
      "default": false
    },
    "unregisterOnShutdown": {
      "title": "Unregister On Shutdown",
      "description": "Unregister all devices on shutdown. Only for debugging purposes, not recommended for production use.",
      "type": "boolean",
      "default": false
    }
  }
}
